diff --git a/headroom/cli/wrap.py b/headroom/cli/wrap.py
index 94cbcf7..d3da297 100644
--- a/headroom/cli/wrap.py
+++ b/headroom/cli/wrap.py
@@ -1571,5 +1571,6 @@ def _ensure_serena_dashboard_disabled(*, verbose: bool = False) -> None:
     cfg = Path.home() / ".serena" / "serena_config.yml"
     key = "web_dashboard_open_on_launch"
+    projects_pattern = re.compile(r"^projects\s*:", re.MULTILINE)
     try:
         if cfg.exists():
             text = cfg.read_text(encoding="utf-8")
@@ -1578,6 +1579,8 @@ def _ensure_serena_dashboard_disabled(*, verbose: bool = False) -> None:
                 new = pattern.sub(rf"\g<1>{key}: false", text)
             else:
                 new = text.rstrip("\n") + f"\n{key}: false\n"
+            if not projects_pattern.search(new):
+                new = "projects: []\n" + new
             if new != text:
                 cfg.write_text(new, encoding="utf-8")
                 if verbose:
@@ -1585,7 +1588,8 @@ def _ensure_serena_dashboard_disabled(*, verbose: bool = False) -> None:
         else:
             cfg.parent.mkdir(parents=True, exist_ok=True)
-            # Serena fills defaults for any keys we omit, so a single-key file is valid.
-            cfg.write_text(f"{key}: false\n", encoding="utf-8")
+            # Serena fills defaults for optional keys, but current releases
+            # require the projects key to be present.
+            cfg.write_text(f"projects: []\n{key}: false\n", encoding="utf-8")
             if verbose:
                 click.echo("  Serena: created serena_config.yml with dashboard auto-open off")
     except OSError as e:
diff --git a/headroom/mcp_registry/install.py b/headroom/mcp_registry/install.py
index 6943e71..8393a67 100644
--- a/headroom/mcp_registry/install.py
+++ b/headroom/mcp_registry/install.py
@@ -5,1 +5,2 @@
+import sys
 from collections.abc import Iterable
@@ -59,6 +60,9 @@ def build_serena_spec(context: str) -> ServerSpec:
         name="serena",
         command="uvx",
         args=(
+            "--python",
+            sys.executable,
+            "--no-python-downloads",
             "--from",
             "git+https://github.com/oraios/serena",
             "serena",
diff --git a/headroom/providers/proxy_routes.py b/headroom/providers/proxy_routes.py
index 3b767d1..af31ec1 100644
--- a/headroom/providers/proxy_routes.py
+++ b/headroom/providers/proxy_routes.py
@@ -25,6 +25,7 @@ from headroom.providers.openai_responses import (
     OpenAIResponsesSubpathRoute,
     handle_openai_responses_subpath,
 )
+from headroom.proxy.project_context import set_current_project
 from headroom.providers.proxy_targets import (
     _api_target,
 )
@@ -131,5 +132,16 @@ def _register_openai_responses_websocket_route(app: FastAPI, proxy: Any, path:
     async def openai_responses_ws(websocket: WebSocket):
         await proxy.handle_openai_responses_ws(websocket)
-
+
+    async def openai_responses_ws_project(websocket: WebSocket, project: str):
+        # HTTP middleware strips /p/<project> before route matching, but it
+        # does not run for WebSockets. Match the prefixed URL explicitly and
+        # seed attribution for the handler task.
+        set_current_project(project)
+        await proxy.handle_openai_responses_ws(websocket)
+
     openai_responses_ws.__name__ = path.strip("/").replace("/", "_").replace("-", "_") + "_ws"
+    openai_responses_ws_project.__name__ = (
+        path.strip("/").replace("/", "_").replace("-", "_") + "_project_ws"
+    )
     app.websocket(path)(openai_responses_ws)
+    app.websocket(f"/p/{{project}}{path}")(openai_responses_ws_project)
diff --git a/headroom/proxy/handlers/openai.py b/headroom/proxy/handlers/openai.py
index 70148f2..3b9e05d 100644
--- a/headroom/proxy/handlers/openai.py
+++ b/headroom/proxy/handlers/openai.py
@@ -76,1 +76,5 @@
-from headroom.proxy.project_context import classify_project, set_current_project
+from headroom.proxy.project_context import (
+    classify_project,
+    get_current_project,
+    set_current_project,
+)
@@ -5490,7 +5494,7 @@ class OpenAIHandler:
         client = classify_client(ws_headers)
         # WS sessions bypass the HTTP middleware, so bind the project here;
         # per-turn outcome emission inside this task inherits the context.
-        set_current_project(classify_project(ws_headers))
+        set_current_project(classify_project(ws_headers) or get_current_project())
         metrics_for_inbound_ws = getattr(self, "metrics", None)
         if metrics_for_inbound_ws is not None and hasattr(
             metrics_for_inbound_ws, "record_inbound_request"
