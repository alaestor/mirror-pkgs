diff --git a/solidlsp/language_servers/json_language_server.py b/solidlsp/language_servers/json_language_server.py
--- a/solidlsp/language_servers/json_language_server.py
+++ b/solidlsp/language_servers/json_language_server.py
@@ -53,6 +53,12 @@ class JsonLanguageServer(SolidLanguageServer):
             """
             Setup runtime dependencies for JSON Language Server and return the path to the executable.
             """
+            system_json_language_server = shutil.which("vscode-json-language-server") or shutil.which(
+                "vscode-json-languageserver"
+            )
+            if system_json_language_server is not None:
+                return system_json_language_server
+
             is_node_installed = shutil.which("node") is not None
             assert is_node_installed, "node is not installed or isn't in PATH. Please install NodeJS and try again."
