diff --git a/solidlsp/language_servers/marksman.py b/solidlsp/language_servers/marksman.py
--- a/solidlsp/language_servers/marksman.py
+++ b/solidlsp/language_servers/marksman.py
@@ -5,6 +5,7 @@ Contains various configurations and settings specific to Markdown.
 
 import logging
 import os
+import shutil
 from collections.abc import Hashable
 
 from overrides import override
@@ -120,6 +121,10 @@ class Marksman(SolidLanguageServer):
 
         def _get_or_install_core_dependency(self) -> str:
             """Setup runtime dependencies for marksman and return the command to start the server."""
+            system_marksman = shutil.which("marksman")
+            if system_marksman is not None:
+                return system_marksman
+
             marksman_version = self._custom_settings.get("marksman_version", DEFAULT_MARKSMAN_VERSION)
